{% extends "baseform.html.twig" %}

{% block title %}Modifier un trick{% endblock %}

{% block contact %}
    <section class="page-section" id="contact">
        <div class="container">
            <div class="text-center">
                <h2 class="section-heading text-uppercase">Modifier un trick - {{ trick.name }}</h2>
                <h3 class="section-subheading text-muted"></h3>
            </div>
            <div class="row align-items-stretch mb-5 justify-content-md-center">
                <div class="col-md-6 form-group" >
                    {{ form(form, {"attr": {"id":"contactForm"}} ) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}